extends layout

block content
  h1= message
  if error.status == 404
    h2 404 - Page Not Found
    p Sorry, the page you are looking for does not exist.
  else
    h2= error.status
    p #{error.stack}
